# 주택 매수 의사결정 시뮬레이터

`simulator/` 에 있는 React + TypeScript 앱이 이 저장소의 주 작업물입니다.
루트의 `index.html` 은 무관한 옛 Hello World 파티클 페이지이니 건드리지 마세요.

**엔트리는 하나입니다.** 화면은 탭으로 늘리고 번들은 하나로 둡니다.

전월세 찾기를 두 번째 엔트리(`rent.html`)로 둔 적이 있는데, 그 순간 Vite 가
공통 코드를 **공유 청크**로 분리했습니다. 단일 HTML 빌더는 `<script src>` 태그만
인라인하므로 엔트리 코드 안의 `import "./index-*.js"` 가 그대로 남았고, 배포된
문서가 그 파일을 못 찾아 **두 사이트가 모두 백지**가 됐습니다. 파일 크기는
3.4MB 로 멀쩡했고 외부 http 참조도 없어서 채점표도 못 잡았습니다.

지금은 `＋ 청약 · 전월세` 탭 안에서 둘을 오갑니다. 엔트리를 늘리려면 먼저
**인라인 문서에 미해결 모듈 참조가 없는지** 검사가 통과하는지 보세요.

## 이 도구가 존재하는 이유

기존 부동산 계산기는 **대출과 물건을 따로 봅니다.** 그런데 두 축은 얽혀 있습니다.

```
경기권    = 수도권   = LTV 70%  (생애최초 우대 무효)
창원·부산 = 비수도권 = LTV 80%  (생애최초 우대 유효)
```

**지역 선택이 곧 대출 조건 선택**이고, 이 커플링을 화면에 드러내는 것이 존재 이유입니다.
대상 지역은 **창원 · 부산 · 경기 수도권 3개로 고정**입니다. 전국으로 넓히지 마세요.

## 아키텍처 — 두 가지 분리 원칙

```
src/rules/2026-08.json   ← 모든 정책 수치. 코드엔 로직만.
src/data/regions.json    ← 수집 대상·후보군과 그 이유 (수집 스크립트가 읽음)
src/data/market-YYYY-MM.json ← 매매 실거래 스냅샷 (빌드 타임 수집, 손대지 말 것)
src/data/rent-YYYY-MM.json   ← 전월세에서 잰 전세가율·전환율
src/data/presale-YYYY-MM.json ← 분양권전매 실거래 스냅샷
src/data/applyhome-YYYY-MM.json ← 청약홈 공고·주택형·1순위 경쟁률 (엔진 import 파일명 고정)
src/data/masan-rent-YYYY-MM.json ← 마산 생활권 아파트 전월세 실거래 (두 번째 사이트)
src/data/board-YYYY-MM.json ← 전·월세 후보판 스냅샷 (행·도로거리·시설 수·지도 조각·첫 설정) — scripts/board/
src/data/index-*.json · rates-*.json ← KRX·FRED·ECOS 지수와 금리
src/engine/              ← 순수 TS. UI import 금지. 단위테스트 대상.
  rules.ts       룰셋 로더, 지역·규제지역 판정
  finance.ts     원리금균등 (PMT / PV)
  scenario.ts    프로필 × 시나리오축 → 파생 상태
  costs.ts       취득세·중개보수·부대비용
  loan.ts        한도 파이프라인 + 상품 랭킹 + 제약 조언
  leverage.ts    레버리지 스프레드 방향 분석
  tax.ts         재산세·양도세·임대차 중개보수 (보유/처분 단계)
  tenure.ts      매수·전세·월세 3-way 종료자산 비교 (월별 시뮬레이션)
  affordability.ts  상품별 최대 감당가격 역산 ("얼마까지 되나")
  thesis.ts      물건 성격 판정 (신축 / 재건축 기대 / 애매 구간)
  market.ts      실거래 스냅샷 로더 + CAGR + 진입시점 분포 + 보유표본(holdingSamples)
  growth.ts      지역·시장 연쇄 가격지수 (집값 상승률 실측)
  backtest.ts    롤링 백테스트 — 진입시점 축 초과수익 + 시점 분할 검증
  supply.ts      향후 입주 물량 대리지표 (분양권 vs 매매)
  population.ts  권역 인구 추세 실측 (시도 단위)
  repair.ts      장기수선충당금 실측 (수선유지비 하한 앵커)
  offering.ts    청약 공고 안전마진 (분양가를 네 자로 견줌)
  handbook.ts    대출 설명서 — 룰셋을 사람 말로 펴는 곳 (좌측 서랍)
  rating.ts      공고 별점 — 네 축을 나란히 (합산 금지)
  applyhome.ts   청약홈 공고 로더 — 분양가·평형·일정 자동 채움 + 1순위 경쟁률
  subscription.ts 청약 납입 일정 + 4번째 갈래 LegPlan
  rent.ts        전세가율·전월세전환율 실측치 (자리표시자 대체)
  regions.ts     시군구 목록·후보군 + 시군구명 → 법정동코드 조회
  ranking.ts     수익률 상위권 추출 + 공통점(상위권 진입 확률) + 리포트 생성
  presale.ts     분양권 프리미엄 (분양권 전매가 vs 준공 후 매매가)
  scoring.ts     입지 지표 정규화 + 지역별 가중치 프리셋
  matrix.ts      매트릭스 조립 + 3축 산점도 데이터
src/rules/rent-loans-YYYY-MM.json ← 전월세 보증금 대출 룰셋 (매수 룰셋과 별개)
src/rentfinder/          전월세 찾기 — `＋ 청약 · 전월세` 탭 안의 한 갈래
  data.ts        전월세 스냅샷 로더 + 출퇴근 등급
  finder.ts      조건 필터 + 월 환산 주거비 + 정렬 (UI 를 모릅니다)
  loans.ts       전세대출 자격·한도 비교 + 다음 행동 조언 + 후보별 되는 상품(jeonseLoanFit)
  safety.ts      전세가율 · 근저당 허용선 · 매매가 추세 · 등기부 확인 목록
  RentFinderApp.tsx  화면 — 매수 쪽 스토어를 공유하지 않습니다
  board/template.html 전·월세 후보판 원본 (아티팩트와 같은 파일) → template.ts 는 자동 생성
  board/boardDoc.ts   스냅샷을 채워 문서 한 장으로 조립 + 내 조건으로 대출 칸 다시 재기 (React 모름)
  boardRow.ts         후보판 한 줄 규칙 · 최근 전세 · 상품별 최대 보증금 · 후보판에 없는 집 찾기
  BoardFrame.tsx      후보판을 iframe srcdoc 으로 싣는 자리
src/state/store.tsx      React 상태 + localStorage
src/pages/               화면
```

1. **정책 수치는 JSON에만.** 규정이 자주 바뀌므로 코드를 고치지 않고 JSON 교체로 대응합니다.
   룰셋에는 `version`, `effectiveFrom`, `sunset` 이 필수이고 화면 하단에 기준일이 상시 노출됩니다.
2. **엔진은 UI를 모릅니다.** `engine/` 안에서 React를 import하지 마세요. 계산은 전부 여기서 끝나야 테스트가 가능합니다.

## 한도 계산 파이프라인

```
STEP 1 자격 게이트    소득·자산·주택가격·면적·생애최초·연령·시행일
STEP 2 LTV           수도권 여부 → 규제지역 여부 순
STEP 3 상품 캡        단독세대주 축소 한도, 수도권 절대상한
STEP 4 상환능력       DSR 면제 상품은 DTI, 은행 상품은 DSR + 스트레스 금리
STEP 5 최종          min(위 넷) + bindingConstraint 판별 + 필요소득 역산
```

**한도가 막혔다는 사실만 알려 주면 절반만 답한 것입니다.** 다음 행동은 "그래서
얼마를 더 벌어야 하나" 이므로, 상환능력을 뺀 최대치(`limitBeforeRepay`)와 그
금액을 받아 내는 데 필요한 연소득(`requiredIncome`)을 같이 냅니다.

```
limitBeforeRepay = min(LTV, 상품캡, 가격상한)      ← 소득이 늘면 여기까지
requiredIncome   = (원리금 + 기존부채) ÷ DTI or DSR  ← STEP 4 를 그대로 뒤집음
```

두 가지가 함정입니다.

- **스트레스 금리로 재야 합니다.** 실제 금리로 뒤집으면 필요소득이 작게 나와,
  그만큼 벌어도 한도가 안 열립니다.
- **금리가 소득을 따라 움직여 한 번에 안 풀립니다.** `effectiveRate` 는 소득이
  자격 상한에 가까울수록 금리를 올리므로 "필요소득 → 금리 → 원리금 → 필요소득"
  이 서로를 뭅니다. 몇 번 돌려 수렴시키고 만원 단위로 올림합니다 — 딱 떨어지는
  값을 적으면 그 소득을 벌어도 한 뼘 모자랍니다.

이 값은 **세 곳**에 나옵니다 — 매트릭스 셀(열지 않아도 한 줄), 셀 상세(부족액과
자격 상한 대조), 대출 설명서(상품 한도별 필요소득). 설명서는 물건을 받지 않고
**상품 자체의 한도**를 기준으로 재므로 "이 상품을 끝까지 쓰려면 얼마를 벌어야
하나" 에 답합니다. 어느 시나리오의 판정소득인지는 서랍 머리에 적습니다.

**주택가격 상한과 대출 절대상한은 다른 축입니다.** 보금자리론은 6억짜리까지
살 수 있지만 빌려주는 돈은 4.2억에서 멈춥니다 — "6억까지 가능" 이 "6억을
빌려준다" 로 읽히는 자리라, 가격 나침반이 **대출이 멈추는 가격**을 축 위에
노란 선으로 찍습니다 (`loanCeilingPrice`).

```
대출액 = min(LTV×가격, 상품캡, 상환능력, 가격상한)
         ↑가격에 비례      ↑둘 다 가격과 무관
천장가격 = min(상품캡, 상환능력) ÷ LTV
```

창원 · 생애최초 기준으로 **5.25억부터 대출이 4.2억에서 멈추고**, 그 위로 오른
가격은 한 푼도 대출로 안 채워집니다. 같은 줄이 **대출 설명서**에도 지역·LTV별로
붙고(`loanCeilingSection`), 전세 항목에는 "대출이 멈추는 보증금 5.00억" 으로
같은 구조를 냅니다 — 전세도 보증금이 오르면 대출은 4억에서 멈춥니다.

값이 같은 지역은 한 줄로 합칩니다. 은행 상품은 세 지역 LTV 가 모두 70% 라
따로 적으면 똑같은 줄이 셋이고, 그러면 "왜 셋을 나눠 놨지" 를 먼저 고민하게
됩니다.

**정책상품에는 소득으로 뚫을 수 없는 구간이 있습니다.** 소득이 낮아야 자격이
나오고 높아야 한도가 나오는데, 필요소득이 자격 상한을 넘으면 소득을 올리는 순간
상품을 잃습니다(`requiredIncomeBlocked`). "소득이 부족하다" 로만 적으면 사용자가
없는 길을 향해 뜁니다.

**생애최초는 상품마다 성격이 다릅니다 — 요건인 곳과 우대인 곳이 있습니다.**

```
디딤돌      requires_first_time: true   → 아니면 탈락
보금자리론  요건 아님                    → 받되 LTV 70→80%, 한도 3.6→4.2억이 갈림
신생아 특례 요건 아님 (신생아 요건)
은행 주담대 우대 없음
```

우대값을 무조건값으로 박아 두면 생애최초가 아닌 사람에게도 우대가 나갑니다.
실제로 보금자리론이 그렇게 들어가 있었고(이름이 "(생애최초)" 라 가려져 있었습니다),
`ltv_first_time_non_capital` · `cap_first_time` 두 단으로 갈라 고쳤습니다.

`bindingConstraint` 노출이 UX의 핵심입니다. "3.2억"만 보여주면 왜 그 숫자인지 모릅니다.
무엇이 막았는지와 **다음 행동**을 함께 표시하되, 조언은 물건 맥락을 반영해야 합니다 —
이미 비수도권 80%를 받는 물건에 "비수도권으로 바꾸라"고 하면 안 됩니다.

## 레버리지 스프레드

```
r_equity ≈ r_asset + (L/E) × (r_asset − i)
                      ↑배율      ↑스프레드
```

배율은 곱셈, 금리는 스프레드 안의 한 항입니다. 따라서 **부호가 1차 관문**입니다.
가격상승률이 금리를 밑돌면 LTV가 높을수록 손실이 커집니다 — 비수도권 80% 우대가 독이 됩니다.
이 근사는 원금상환·보유세·거래비용·양도세를 제외했으므로 **수익률 예측치가 아니며**, UI에 그렇게 명시돼 있습니다.

## 화면 구조 — 탭 6개 + 왼쪽 서랍

```
1 프로필 · 시나리오 · 물건   입력. 한 번 채우고 잘 안 건드리므로 한 탭 안에서 단계로 오갑니다
2 비교 매트릭스              시나리오 × 물건 — 무엇이 되나
3 매수 · 전세 · 월세         같은 자기자본에서 출발한 4-way 종료자산
＋ 청약 · 전월세             선택 둘. 청약(공고·안전마진·별점) / 전월세(찾기·보증금 대출)
4 실거래 수익률              단지 실측 · 롤링 백테스트 · 시점 분할 검증
5 리포트                     인쇄용 요약

◂ 대출 설명서 (좌측 서랍)    룰셋을 그대로 편 규정 설명. 어느 화면에서든 열립니다
```

**설명서는 왼쪽입니다.** 오른쪽 서랍은 곁가지 분석에 쓰던 자리였고, 이건 성격이
다릅니다 — 매트릭스가 "3.2억 · 은행 주담대" 라고 답했을 때 **그 상품이 무엇을
요구하는지**를 본문을 떠나지 않고 펴 보는 자리입니다. 상품명·전세 가정값이
설명서의 해당 항목으로 바로 건너뜁니다.

`handbook.ts` 에는 **규정 숫자가 하나도 없습니다.** 어떤 항목이 무슨 뜻인지만
적고 값은 전부 룰셋에서 읽습니다. 라벨을 못 붙인 키가 생기면 키 이름 그대로라도
내보내고 테스트가 걸어 냅니다 — **조용히 빠뜨리면 "그 조건이 없다" 로 읽힙니다.**

### 뺀 것과 그 이유

| 뺀 화면 | 이유 |
|---|---|
| 수익률·청약 공통점 드로어 | 시점 분할 검증이 그 답의 절반을 무효화했습니다 (재현율 52%). 같은 질문을 백테스트가 더 정직하게 답합니다 |
| 3축 산점도 | 매트릭스와 같은 데이터의 다른 그림. 조합이 12개면 점보다 표가 낫습니다 |
| 분양권 프리미엄(실거래 탭) | 지우지 않고 **청약 탭으로 옮겼습니다** — 떨어졌을 때의 대안이라 거기가 제자리입니다 |

기준은 하나입니다: **그 화면이 행동을 바꾸는가.** 못 바꾸면 흥미로운 숫자일 뿐입니다.

## 채점표

`npm run scorecard` 가 이 저장소의 약속을 자동으로 잽니다 (100점 만점).

```
구조 36  탭 6개 이하 · 겹치는 화면 없음 · 고아 파일 없음 · 계산↔UI 분리 · 화면이 룰셋 직독 안 함
설명서 16  좌측 서랍 장착 · 설명서 코드에 규정 숫자 없음
연결 17  상품명→설명서 · 물건→실거래 · 3-way→전세 규정
검증 18  타입체크 · 단위 테스트 전부
산출물 13  단일 HTML 빌드 · **미해결 모듈 참조 0건** · charset 선두 · 외부 요청 0건
```

**건너뛴 검사는 분자에서도 분모에서도 뺍니다.** 안 돌린 검사에 점수를 주면
`--fast` 가 늘 만점이 되고, 그 순간 채점표가 자기를 속이는 자가 됩니다.
"화면이 깔끔한가" 같은 항목은 넣지 않았습니다 — 통과시키려고 기준을 낮추게 됩니다.

## 지켜야 할 설계 원칙

- **점수를 하나로 합치지 마세요.** 입지 88점 + 대출 적합 60점 = 74점은 무의미합니다. 축별로 봅니다.
- **부정적 결과를 숨기지 마세요.** "자금 부족" 조합과 상품별 탈락 사유가 가장 중요한 정보입니다.
- **가중치는 사용자가 조절**합니다. 지역 프리셋은 출발점이지 정답이 아닙니다.
- **입지 점수는 주관적 입력의 가중합**입니다. 객관적 시세 예측이 아님을 UI에 계속 명시하세요.
- 목적함수 중 월납·이자 최소화는 **덜 빌리는 쪽**을 유리하게 만듭니다. 필요현금과 함께 읽어야 합니다.

## 명령

```bash
cd simulator
npm run dev              # 개발 서버 localhost:5173
npm test                 # 엔진 단위 테스트 (현재 528건)
npm run scorecard        # 채점표 — 구조·연결·검증·산출물 100점 만점
npm run typecheck
npm run deploy:realty    # 빌드 → 루트 realty/index.html (GitHub Pages /realty/)
npm run standalone       # dist/standalone.html — 골격 없는 조각 (아티팩트 호스트용)
npm run fetch:market     # 국토부 매매 실거래가 → src/data/market-*.json
npm run fetch:rent       # 국토부 전월세 실거래가 → src/data/rent-*.json
npm run fetch:index      # KRX 코스피·채권지수 → src/data/index-*.json
npm run fetch:rates      # FRED 해외지수 + ECOS 금리·물가 → src/data/rates-*.json
npm run fetch:presale    # 국토부 분양권전매 실거래가 → src/data/presale-*.json
npm run fetch:dividend   # ECOS 코스피 배당수익률 → src/data/dividend.json
npm run fetch:population # 행안부 통계연보 지역별 인구 → src/data/population.json
npm run fetch:repair     # K-apt 장기수선충당금 → src/data/repair.json
npm run fetch:applyhome  # 청약홈 분양정보·경쟁률 → src/data/applyhome-*.json
node ../scripts/fetch-masan-rent.mjs   # 마산 생활권 전월세 → src/data/masan-rent-*.json
node ../scripts/fetch-masan-landlord.mjs  # 갭투자 흔적 재현용 (결과는 잡음 — 앱에 안 씀)
node ../scripts/calc-newbuild-floor.mjs --write   # 신축 하한 재계산 → 룰셋
```

수집 대상은 `src/data/regions.json` 의 `collect: true` 뿐입니다. 후보군과 제외 사유도
같은 파일에 있어, 무엇을 왜 안 받는지가 코드가 아니라 데이터로 남습니다.
`.env.local` 의 `MOLIT_API_KEY` 는 **Encoding 키**(`%` 포함)입니다. URL 에 그대로
붙여야 하고 **한 번 더 인코딩하면 안 됩니다** — `%2B` 가 `%252B` 가 되어
`SERVICE_KEY_IS_NOT_REGISTERED` 로 돌아옵니다. 이 오류 문구는 "키가 미등록" 이라고
말하지만 실제로는 인코딩 사고인 경우가 있으니, **이미 되던 API 로 먼저 대조**하세요.

**법정동코드는 행정구역 개편으로 바뀝니다** — 화성시 41590 은 폐지돼 동탄·병점·만세·
효행으로 갈렸고, 26260 을 수영구로 적어 뒀다가 실제로는 동래구였던 적도 있습니다.
폐지된 코드는 오류가 아니라 `totalCount=0` 으로 조용히 돌아오니, 수집 후 지역별
건수를 반드시 확인하고 라벨은 API 의 `estateAgentSggNm` 로 대조하세요.

엔진을 고쳤으면 **반드시 `npm test`** 를 돌리세요. 도메인 규칙이 테스트에 박혀 있습니다.

## 배포

| 대상 | 방법 | 주소 |
|---|---|---|
| GitHub Pages | `npm run deploy:realty` → 커밋·푸시 | `/Hello_World/realty/` |
| 자체 AWS 서버 | `./scripts/deploy-aws.sh` | `<서버>/hyrealty/` |

**배포 전에 빌드 산출물이 실제로 렌더되는지 봐야 합니다.** 크기와 존재만 보면
백지를 배포하게 됩니다 — 실제로 그랬습니다. `.claude/launch.json` 의
`built-realty` 가 `realty/` 를 정적으로 띄우니 거기서 `#root` 가 채워지는지
확인하세요. 채점표의 "인라인 문서에 미해결 모듈 참조가 없음" 이 그 사고를
정적으로 잡습니다.

빌드 결과는 CSS·JS를 전부 인라인한 **외부 요청 0건의 단일 HTML** 입니다.
그래서 경로에 무관하게 동작하고, 오프라인·파일 직접 열기도 됩니다.
`<meta charset>` 이 문서 첫 1024바이트 안에 있어야 합니다 — 없으면 한글이 전부 깨집니다.

## 비밀정보 취급

- **자격증명을 커밋하지 마세요.** `.gitignore` 가 `AWS.txt`, `.env*`, `*.pem`, `*secret*` 등을 막고 있습니다.
- 서버 접속 정보는 `.env.deploy` (gitignore됨). 형태는 `.env.deploy.example` 참고.
- **이 저장소는 공개입니다.** 서버 IP·키 경로도 코드에 넣지 마세요.
- **API 키를 프론트엔드에 넣을 수 없습니다.** 정적 단일 HTML이라 문서에 담긴 값은 누구나 읽습니다.
  외부 데이터가 필요하면 **빌드 타임에 받아 JSON으로 굽는** 방식을 쓰세요.

## 다음 작업 (합의된 로드맵)

1. ~~**3-way 거주형태 비교**~~ — **완료** (`tenure.ts`, `tax.ts`, `pages/TenurePage.tsx`).

   세 갈래가 같은 자기자본 E에서 출발해 N년 뒤 **종료자산**을 비교합니다.
   월별 시뮬레이션이라 만기 도래·갱신 증액처럼 중간에 바뀌는 흐름을 담습니다.
   ```
   매수 = P_end − 잔여대출 − 매도중개보수 − 양도세 + 주식잔고
   임차 = 보증금 반환 − 임차대출 + 주식잔고
   주식잔고 = (E − 초기투입) 성장 + Σ(기준예산 − 내 월지출) 적립 성장
   ```
   - **원금상환 대칭**이 핵심입니다. 매달 세 갈래 중 가장 많이 쓰는 쪽을 기준예산으로 잡고
     각자 덜 쓴 만큼 적립합니다. 기준예산에 상수를 더해도 세 갈래에 같은 금액이 더해지므로
     **격차와 순위는 불변**입니다 — 테스트로 고정해 뒀습니다.
   - **월세는 전세에서 파생**됩니다. 전세보증금 차액 × 전월세전환율. 독립 가정값이 아닙니다.
   - **`breakEvenPriceGrowth`가 이 화면의 핵심 숫자**입니다. "10년 뒤 얼마"보다
     "몇 % 올라야 매수가 이기나"가 검증 가능한 질문입니다.
   - 갱신 증액 보증금은 주식을 헐어 충당한다고 봅니다 (대출 증액 아님).

   화면은 ① 목돈을 어디에 두나 ② N년 동안 매달 ③ N년 뒤 손에 남는 것 세 단계로 갈라
   보여줍니다. 목돈과 매달을 시간축으로 분리하지 않으면 "투자 0원인데 적립 7천만"이
   모순처럼 읽힙니다.

   전세가율·전월세전환율은 2단계에서 **실측으로 교체됐습니다**. 남은 자리표시자는
   투자수익률·상승률·수선유지비입니다.

2. **데이터 파이프라인** — 국토부 매매 실거래가는 **완료**. `scripts/fetch-market.mjs` 가
   빌드 타임에 받아 `src/data/market-YYYY-MM.json` 으로 굽고, 화면은 스냅샷만 읽습니다.

   ```bash
   npm run fetch:market                          # 기본 범위
   npm run fetch:market -- --regions 48123       # 창원 성산구만
   ```

   키는 `.env.local` 의 `MOLIT_API_KEY` (gitignore). **저장소가 공개이고 결과물이 단일
   정적 HTML이라 런타임에 키를 둘 수 없습니다.** 스냅샷은 통째로 HTML에 인라인되므로
   바이트가 곧 로딩 시간입니다 — 분기 중위가로 접고, 분기를 정수 인덱스로 저장합니다.
   해제(`cdealType=O`)된 거래는 제외합니다. 체결되지 않은 신고가 섞이면 고점이 왜곡됩니다.

   **전월세도 완료** — `scripts/fetch-rent.mjs` → `src/data/rent-YYYY-MM.json`.
   전월세 자료에는 매매와 **같은 `aptSeq`** 가 있어, 시장 평균끼리 나누지 않고
   **같은 단지·평형·분기**끼리 짝지어 비율을 냅니다. 시장 평균으로 나누면 전세가
   활발한 단지와 매매가 활발한 단지가 뒤섞여 실제로 없는 비율이 나옵니다.

   ```
   전세가율     = 전세보증금 ÷ 같은 칸 매매 중위가
   전월세전환율 = (월세×12) ÷ (같은 칸 전세보증금 중위 − 월세보증금)
   ```

   `defaultAssumptions` 가 실측치를 우선 쓰고 룰셋 값은 폴백으로만 남습니다.
   실측 결과 **부산 전세가율이 자리표시자 68%가 아니라 56.4%** 였고, 이 하나로
   부산의 3-way 결론이 매수 → 월세로 뒤집혔습니다. 자리표시자가 답을 만들고
   있었다는 뜻입니다.

   남은 소스:
   - ~~국토부 **분양권전매** 실거래가~~ — **완료**. `presale.ts` 가 같은 단지·평형끼리
     짝지어 프리미엄을 냅니다. 분양권 자료에는 `aptSeq` 가 없어 단지명·법정동·평형으로
     조인하고, **매매가 분양권 거래 이후인 건만** 씁니다 — 순서가 뒤집히면 비교가 아닙니다.
   - ~~한국은행 ECOS~~ · ~~KRX~~ · ~~FRED~~ — **완료**. 전세자금대출 금리·CPI·코스피·채권·
     S&P500·나스닥100 이 실측으로 들어왔습니다.

   **다만 총수익지수(TR)는 코스피에 없습니다.** KRX 지수 API 가 주는 코스피 계열 51개가
   전부 가격지수라 배당이 빠집니다. 그래서 가격지수 실측 CAGR 에 **배당수익률 가정**을
   더한 근사치를 쓰고, 화면에 `실측+가정` 표식을 답니다. FRED 의 S&P500·나스닥100 도
   같은 사정이라 같은 방식으로 맞췄습니다. **KRX 채권지수만 `TOT_EARNG_IDX` 라 진짜
   총수익 실측**입니다. 지수마다 제공 기간이 달라(나스닥 1986~, S&P500 2016~) 가장 늦게
   시작하는 지수에 맞춰 구간을 자릅니다 — 40년 CAGR 과 10년 CAGR 을 나란히 놓으면
   비교가 성립하지 않습니다.
   - 네이버 부동산은 제외. 공식 API 없고 약관상 크롤링 금지이며 판례도 있습니다.

3. ~~**롤링 백테스트**~~ — **완료** (`backtest.ts`, `MarketPage` 의 진입시점 축 카드).

   단지 하나를 놓고 보던 `holdingDistribution` 을 스냅샷 전체로 넓혔습니다.
   모든 단지·평형 × 모든 진입분기 = 5년 보유 기준 표본 14,462건(2,161칸 × 24분기).

   **초과수익 기준선을 지수가 아니라 코호트로 둡니다.** 연쇄 지수는 분기 중위
   변화율을 곱해 이어 붙이면서 위로 치우칩니다(연쇄 드리프트) — 5년 보유 기준
   시장지수 중위 8.06% 대 표본 중위 4.13% 로, 모든 진입분기에서 지수가 2~4%p
   위에 있습니다. 그 차이를 초과수익이라 부르면 표본의 81% 가 "시장에 못 미쳤다"
   가 되는데 그건 물건의 성적이 아니라 지수의 성질입니다.

   ```
   cohort    같은 진입분기 표본의 중위 (기본) — 정의상 중위 초과수익이 0
   district  같은 진입분기 × 같은 시군구 중위 — 동네 효과까지 제거
   market    수집 3권역 합산 연쇄지수 — 드리프트를 경고와 함께 표시
   none      변환 없음 — "많이 오른 때 들어갔다" 가 1등 공통점이 됩니다
   ```

   **시점 분할 검증**은 진입분기를 표본 수 기준 반으로 갈라(2016Q1~2019Q3 학습 /
   2019Q4~2021Q4 검증), 학습에서 상위권에 몰렸던 구간이 검증에서도 몰리는지 봅니다.
   **상위권은 각 구간 안에서 따로 뽑습니다** — 전체에서 한 번에 뽑으면 상승장이던
   학습 구간이 상위를 독식해 검증이 "시기 맞히기" 가 됩니다.

   결과는 **재현율 52%** 입니다. 학습에서 강했던 42개 구간 중 22개만 검증 구간에서
   기준선 위였습니다. 30년 이상 재건축 기대(3.43배→1.69배)와 초대형(2.03→1.85)은
   남았고, **진입 가격대는 전부 깨졌습니다** — 싼 것이 더 오른 것은 그 시기의
   성질이었습니다. 이 화면의 결론은 "공통점을 찾았다" 가 아니라 **"찾은 공통점의
   절반은 시기였다"** 입니다.

   화면은 진입분기별 **중위(시기)** 막대와 **사분위 폭(선택)** 막대를 나란히 놓습니다.
   폭이 시기 차이보다 크면 언제 들어가느냐보다 무엇을 고르느냐가 더 갈랐다는 뜻입니다.

4. ~~**청약 축**~~ — **완료** (`subscription.ts`, `pages/SubscriptionPage.tsx`).

   매수/전세/월세와 구조가 다릅니다: 계약금 10% → 중도금 60%(이자후불) → 잔금 30% 를
   2~3년에 걸쳐 내고 **그동안은 다른 집에 임차로 삽니다.**

   분양가·전용면적·일정은 **청약홈 공고에서 불러옵니다** (`applyhome.ts`). 남은
   손입력은 **중도금 조건(이자후불 여부·금리)과 전매제한** 둘뿐입니다 — 이 둘은
   API 에 없고 단지 공고문 본문에만 있습니다. 선택 탭이라 비워 두면 화면은
   그대로 3갈래입니다.

   - **의존 방향은 `subscription → tenure`** 입니다. tenure 가 청약을 import 하면
     순환 참조가 되고, 무엇보다 청약은 선택 갈래라 엔진의 기본 축이 아닙니다.
     `LegPlan` 을 공개해 두고 `subscriptionLegPlan()` 이 그 모양으로 빚어 넘깁니다.
   - **기준예산 불변성이 그대로 성립합니다.** 갈래를 하나 더 넣으면 그 달 기준예산이
     올라갈 수 있지만 올라간 만큼이 모든 갈래에 똑같이 더해져 격차와 순위는 불변입니다.
     테스트로 고정해 뒀습니다.
   - **손익분기 상승률은 청약을 빼고 잽니다.** 청약도 집을 사므로 섞으면 둘 다
     가격상승률에 노출돼 분기점이 사라집니다.
   - **중도금 이자는 회차별 잔존기간으로** 쌓습니다. 전액 선차입으로 계산하면
     과장됩니다. 이자후불제는 대기 중 월 부담을 낮추고 입주 목돈을 키우며 총액은 같습니다.
   - **대기 중 주거는 전세가 기본이되, 계약금을 낸 뒤 전세보증금을 못 채우면 월세로
     떨어뜨립니다.** 경고만 띄우고 숫자는 전세로 두면 구할 수 없는 집의 주거비로
     계산한 셈이 됩니다. 월세는 3-way 와 같이 전세에서 파생합니다.
   - **비교가 완전히 대등하지 않습니다.** 나머지 셋은 같은 집인데 청약만 다른 집이고,
     당첨을 전제로 합니다. 이 갈래가 1등이어도 "되면 낫다"까지입니다 —
     `SUBSCRIPTION_CAVEATS` 가 화면에 같이 붙습니다.

5. ~~**스코어링 실측 피드백**~~ — **완료** (`supply.ts`, `population.ts`).

   주관 입력을 **대체하지 않습니다.** 입지 점수가 주관적 입력의 가중합이라는 설계는
   그대로 두고, 실측 신호를 옆에 놓아 **어긋날 때만** 말합니다.

   `futureSupply` 는 분양권 스냅샷으로 잽니다 — 분양권 거래는 있는데 매매가 없는
   단지가 곧 미준공 단지입니다. 화성 동탄 36.2% ↔ 마산회원 3.5% 로 10배 벌어집니다.

   **입력 단위가 세대 수인데 대리지표는 순위밖에 못 냅니다.** 백분위를 세대로
   되돌리면 없는 숫자를 지어내는 것이라 "가져오기" 를 두지 않고, 둘 다 0~100
   점수로 바꿔 방향만 비교합니다. 한계 셋(`SUPPLY_CAVEATS`)이 호버에 붙습니다 —
   세대수가 아니라 단지 수 · 전매제한 단지는 안 잡혀 하한 · 입주 시기 모름.

   `populationTrend` 는 행안부 통계연보로 실측이 들어왔습니다 (`population.ts`).
   **다만 시도 단위**라 창원 성산구와 마산회원구를 가르지 못합니다 — 같은 권역
   시군구는 전부 같은 값을 받으므로 권역 축에서만 씁니다.

### 공고 안전마진 — 완료

새 공고가 뜰 때 **5분 안에 거르는** 화면입니다. 이미 정한 물건을 다른 선택지와
비교하는 4-way 와는 묻는 것이 다릅니다.

`offering.ts` 가 분양가를 **네 자**로 견줍니다. 하나로는 항상 틀립니다.

```
주변 실거래      그 동네에 신축이 없으면 구축과 비교하게 돼 분양가가 늘 비싸 보임
신축 하한 p25    "신축치고도 비싼가" 를 가름. 다만 지역 전체 통계라 동네가 섞임
입주 시점 예상가  살 때의 시세와 비교. CAGR 을 곱하는 순간 예측이 되므로 분포 병기
주변 분양권      가장 비슷한 물건. 다만 전매제한 단지는 안 잡혀 표본이 얇음
```

**점수로 합치지 않습니다.** 넷을 나란히 놓고 **어긋나는 지점**을 보여줍니다 —
창원 성산구 5억 84㎡ 예로 주변 구축 대비 28% 싼데 신축 하한과 견주면 7% 뿐입니다.
그 간격이 곧 "분양가가 신축 프리미엄을 얼마나 미리 당겨 받았나" 입니다.

`margin` 부호는 **양수면 분양가가 그만큼 싸다**입니다 — `(기준가 − 분양가) ÷ 기준가`.

시군구가 비면 권역 전체와 비교하게 되어 숫자가 통째로 달라지므로 화면에
경고를 답니다. 호버에만 적으면 못 보고 지나갑니다.

**공고는 이제 목록에서 고릅니다.** 청약홈 분양정보·경쟁률이 승인돼
`scripts/fetch-applyhome.mjs` 가 스냅샷을 굽고 `applyhome.ts` 가 읽습니다.

```
자동  분양가 · 전용면적 · 시군구 · 법정동 · 세대수 · 계약일 · 입주예정월 · 1순위 경쟁률
손    중도금 이자후불 여부 · 중도금 금리 · 전매제한
```

**불러오기가 납입 구조를 건드리지 않습니다.** 그 셋은 API 에 없어 공고문을 보고
넣은 값인데, 주택형을 바꿔 다시 불러올 때 덮어쓰면 조용히 지워집니다. 테스트로
고정해 뒀습니다.

주의할 것 셋입니다.

- **주택형 `084.9800A` 의 앞 숫자가 전용면적**입니다. `SUPLY_AR` 은 공급면적이라
  20㎡ 넘게 큽니다 — 이 도구의 면적 축은 전부 전용이라 섞으면 안 됩니다.
- **`지구` 로 끝나는 주소 토큰을 구로 보면 안 됩니다.** `평택시 고덕국제화계획지구`
  는 시군구가 아닙니다. 그리고 `경기도 화성시 반월동` 처럼 **구 없이** 오는 공고는
  라벨이 정확히 맞을 때만 붙이고 아니면 비웁니다 — 동탄에 붙여 버리면 기준가가
  통째로 다른 동네 것이 됩니다.
- **경쟁률은 1순위 접수건수 ÷ 공급세대**입니다. API 의 `CMPET_RATE` 는 해당지역·
  기타지역으로 갈려 한 숫자가 아니고 미달이면 `-` 로 옵니다. **1 미만은 0이 아니라
  미달**이고, 창원은 1순위 미달이 46%입니다.

당첨 확률은 내지 않습니다 — 가점제·추첨제·특별공급 유형이 섞여 있어 경쟁률
분포까지가 이 자료로 말할 수 있는 전부입니다.

**목록이 낡아 보이는 두 가지 이유를 화면에서 갈라 줍니다.** 스냅샷이 오래된
것과 그 권역에 새 공고가 없는 것은 다른 문제인데, 구분이 안 되면 "기능이
사라졌나" 로 읽힙니다. 공고 선택 옆에 **그 권역 최신 공고일**을 적고, 수집
기준일과 45일 넘게 벌어지면 "이 권역은 ○○ 이후 새 공고가 없습니다 (수집 기준
△△ · N일째)" 를 띄웁니다. 실제로 창원은 2026-07-16 이후 52일째 신규 공고가
없고, 부산도 07-30 이 마지막입니다 — 경기만 09-04 에 새 공고가 들어왔습니다.

스냅샷은 `npm run fetch:applyhome` 으로 갱신하고, 파일명이 `applyhome-YYYY-MM`
이라 달이 바뀌면 **`applyhome.ts` 의 import 를 같이 고쳐야** 합니다.

### 공고 별점 — 고르는 자리에서 바로

공고를 고른 **그 자리**에서 중도금 조건·전매제한을 넣고, 넣는 즉시 별점이 다시
계산됩니다(`rating.ts`). 단지를 추가한 뒤에 넣게 하면 판단이 끝난 다음에 값을
넣는 셈이라 순서가 거꾸로입니다.

```
분양가       싼가            네 기준 마진 중위 + 권역 공고 대비 ㎡당 백분위
당첨 가능성   붙을 만한가      같은 권역 1순위 경쟁률 분포에서의 위치
자금 부담     지금 낼 수 있나   계약 시점 현금 대비 여유 (중도금 조건이 여기 반영)
묶이는 기간   언제 팔 수 있나   입주 대기 + 전매제한
```

**합산 점수를 아예 내보내지 않습니다.** 내보내면 화면이 그걸 쓰게 되고, 싸지만
붙기 어려운 공고와 비싸지만 붙기 쉬운 공고가 같은 점수가 됩니다 — 둘은 해야 할
일이 정반대입니다. 테스트가 `total`·`score`·`average` 필드의 부재를 고정합니다.

**근거가 없으면 별을 안 줍니다.** 접수 전 공고는 경쟁률이 없으므로 그 축은
`null` 이고 화면에 "잴 수 없음" 으로 나옵니다. 없는 근거로 별 3개를 주면 읽는
사람은 그것도 판정이라고 믿습니다.

축마다 **왜 그 별점인지**가 접이식으로 붙고, 그 축이 못 말하는 것도 같이 답니다
— 경쟁률이 낮은 것은 "붙기 쉽다" 이지 "좋은 물건" 이 아닙니다.

별 옆에는 아이콘이 **둘**입니다. `★` 는 별 5~1 구간표(`bands`)를 열어 지금 값이
든 칸을 파랗게 표시하고, `ƒ` 는 계산식과 대입값(`formula`)을 폅니다. 네이티브
`title` 툴팁은 브라우저가 안 띄우는 경우가 있고 표를 담을 수도 없어, 직접 그린
팝오버(`InfoPopover`)로 바꿨습니다.

**구간은 백분위가 아니라 실제 값으로 적습니다.** "하위 25% 면 ★4" 는 자기 공고를
어디에 놓아야 할지 알 수 없습니다. 묶이는 기간은 개월(`37~48개월`), 경쟁률은
배수(`2.7 : 1 ~ 6.3 : 1`)로 냅니다.

**경쟁률 ★5 는 미달이 통째로 차지합니다.** 미달이 46%인 창원에서 미달까지 섞어
분위수를 내면 하위 구간이 전부 1 아래로 내려가 "미달 아님 ~ 0.1 : 1" 같은 있을
수 없는 칸이 생깁니다. 나머지 네 칸은 **미달이 아닌 표본 안에서** 나누고, 미달이
아니면 ★4 가 상한입니다 — 안 그러면 표와 별이 어긋납니다. 테스트가 "표시된 칸이
곧 그 별" 을 모든 축에서 고정합니다.

**자금 부담 축은 기본으로 접습니다.** 나머지 셋은 공고 자체의 성질인데 이 축만
가구 프로필에 딸려 있어, 공고를 훑는 단계에서는 늘 필요하지 않습니다.

마진은 `(기준가 − 분양가) ÷ 기준가` 라 기준가가 훨씬 쌀 때 음수로 발산합니다.
마산 소형 구축과 신축 분양가를 견주면 −180% 가 나오는데 산술은 맞아도 읽히지
않으므로, 그 구간에서는 **배수**로 말합니다 ("기준가의 2.8배").

### 가정값 현황

`priceGrowthRate` 는 이 도구에서 결론을 가장 많이 흔드는 값입니다 — 매수·청약
갈래만 여기에 노출되므로 이 하나로 승자가 바뀝니다. 그런데 기본값 3% 는 근거가
없었고, **실측은 창원 1.73% · 부산 4.42% · 경기 5.57%** 였습니다.

`growth.ts` 가 실거래 스냅샷에서 잽니다. 분기마다 거래된 단지가 달라 가격 수준을
평균하면 구성 변화가 상승률로 둔갑하므로, 수준이 아니라 **같은 칸(단지·평형)**
을 봅니다. 전월세에서 `aptSeq` 로 짝지은 것과 같은 이유입니다.

**제안값은 연쇄 지수가 아니라 표본 중위입니다** — 지수는 이어 붙이면서 위로
치우치기 때문입니다(아래 "연쇄 지수는 위로 치우칩니다").

**기본값으로 몰래 넣지 않습니다.** 과거 CAGR 을 미래 가정에 자동 대입하면 도구가
"이만큼 오릅니다" 라고 말하는 것이 됩니다. 옆에 놓고 가져다 쓰게 하고, 단일
CAGR 만 내면 진입시점이 감춰지므로 같은 보유기간의 분포를 같이 냅니다.

| 값 | 상태 | 근거 |
|---|---|---|
| 전세가율·전월세전환율 | 실측 | `rent-*.json`, 같은 단지·평형끼리 짝지음 |
| 코스피 배당수익률 | **실측** | ECOS 901Y014 · 20년 평균 1.60% (가정 2.0% 는 25% 과대였음) |
| 집값 상승률 | 실측 앵커 | `growth.ts` — 같은 보유기간 **표본 중위**. 지수는 참고값 (아래) |
| 향후 공급 | 실측 대리지표 | `supply.ts` — 순위만, 세대수 아님 |
| S&P500·나스닥 배당 | 가정 | FRED 에 배당수익률도 총수익지수도 **없음을 확인** |
| 수선유지비 | 하한만 실측 | `repair.ts` — 장기수선충당금 318개 단지. 세대 내부 수선은 여전히 가정 |
| 인구 추이 | **실측** | `population.ts` — 행안부 통계연보, 다만 시도 단위 |
| 분양가·청약 일정 | **실측** | `applyhome.ts` — 청약홈 공고. 중도금 조건·전매제한만 손입력 |

남은 가정값의 근거는 룰셋 `tenure.assumptionDefaults.basis` 에 있습니다 —
무엇을 보고 찍었고 무엇이 있어야 실측이 되는지를 데이터로 남깁니다.
**근거 없는 숫자와 근거 있는 가정은 다릅니다.**

### 연쇄 지수는 위로 치우칩니다 — 제안값을 표본으로 옮겼습니다

`growth.ts` 의 연쇄 지수는 분기마다 같은 칸의 가격비 **중위를 곱해** 이어 붙입니다.
구성 변화는 막아 주지만, 곱셈을 반복하는 동안 위쪽으로 치우칩니다(연쇄 드리프트).

```
5년 보유 · 시장지수 진입시점 중위   8.06%
5년 보유 · 단지·평형 표본 중위      4.13%
```

그래서 **가져다 쓰는 숫자를 지수에서 표본으로 옮겼습니다.** `growthSuggestion()`
은 이제 `market.ts` 의 `holdingSamples()` 로 같은 보유기간을 실제로 들고 있었던
단지·평형의 **중위 CAGR** 을 냅니다. 곱해서 잇지 않고 양 끝을 직접 재므로
치우침이 없습니다. 지수는 모양(차트)과 참고값으로만 남습니다.

| 권역 | 예전 제안 (지수 전체구간) | 지금 제안 (10년 보유 표본 중위) | 같은 10년 지수 중위 |
|---|---|---|---|
| 창원 | 1.73% | **0.03%** (손실 49%) | 1.94% |
| 부산 | 4.42% | **2.88%** | 4.41% |
| 경기 | 5.57% | **3.61%** | 5.57% |

**창원 10년 보유는 중위가 사실상 0%이고 표본의 절반이 손실**이었습니다. 예전
1.73% 는 드리프트와 기간 혼용이 겹친 값입니다.

드리프트는 **같은 보유기간끼리** 재야 합니다 — 10.5년 지수 CAGR 과 5년 표본
중위를 견주면 드리프트가 아니라 기간 차이를 재게 되고, 실제로 그렇게 재면
창원은 부호까지 뒤집힙니다. 화면과 테스트 모두 같은 기간으로 맞춰 뒀습니다.

요청한 보유기간에 표본이 없으면(스냅샷이 2016년부터라 30년은 아예 없습니다)
**표본이 있는 가장 긴 기간으로 물러서고 그 사실을 화면에 냅니다.** 지수 CAGR 로
슬쩍 바꿔치기하면 30년처럼 보이는 다른 숫자가 뜹니다.

### 분석 시 주의

지역이 3개(시군구로 쪼개도 10~15개)라 **통계적 특징 추출 표본이 아닙니다.**
게다가 지역 수익률은 전국 금리·유동성에 함께 끌려다녀 서로 독립이 아닙니다.
특징은 지역 축이 아니라 **진입시점 축**에서 찾고(`backtest.ts`), 같은 진입분기
코호트 대비 초과분으로 변환한 뒤 분석하세요. 검증은 시점 분할로 합니다 —
지금 표본에서는 **재현율 52%** 입니다.

**표본은 서로 독립이 아닙니다.** 같은 칸의 2016Q1 진입과 2016Q2 진입은 보유기간이
거의 겹칩니다. 표본이 1.4만 건이어도 독립 관측은 2,161칸에 가까우므로, 유의성
검정을 붙이지 말고 분포와 재현 여부만 보세요.

## 전월세 찾기 — `＋ 청약 · 전월세` 탭

집을 사기 전까지 살 집을 고르는 화면입니다. 함안·의령 지사 출퇴근을 전제로
마산 생활권 투룸 이상 전월세를 좁힙니다. 매수 시뮬레이터와 **스토어를 공유하지
않습니다** — 저기는 가구 프로필과 시나리오가 필요하고 여기는 예산과 지역만
필요합니다.

**전세와 월세를 한 자에 올립니다.**

```
월 환산 주거비 = 월세 + 보증금 × 기회비용률 ÷ 12
```

보증금은 없어지는 돈이 아니라 **묶이는 돈**입니다. 그 돈이 다른 데서 벌었을
수익(전세대출을 썼다면 그 이자)이 실제 비용이고, 월로 펴야 월세와 견줄 수
있습니다. 기회비용률이 이 화면에서 결론을 가장 많이 흔드는 값이라 화면에서
바꿀 수 있게 두었습니다 (기본 4.2% — 전세대출 금리대).

**매물이 아니라 체결된 계약입니다.** 국토부 신고 자료라 이미 나간 집들입니다.
"이 단지 59㎡가 최근 얼마에 나갔나" 까지 답하고 "지금 빈 집이 있나" 는 답하지
못합니다. 네이버 부동산은 공식 API 가 없고 약관상 크롤링이 금지돼 있어
**시세로 후보를 좁히고 매물 확인은 사람이** 하는 구조입니다.

### 평과 ㎡ — 전용과 공급을 갈라 적습니다

사람은 "15평 아파트" 라고 할 때 대개 **공급면적**을 뜻합니다. 그런데 국토부
전월세 실거래에는 **전용면적만** 옵니다. 전용 40㎡ 를 그대로 평으로 바꾸면
12.1평이라 "15평이 아니네" 가 되는데, 그 집의 공급면적은 15평 안팎입니다.

```
전용 40㎡  =  전용 12.1평  =  공급 약 15평   (전용률 78% 가정)
전용 60㎡  =  전용 18.1평  =  공급 약 23평
```

화면은 **셋을 같이** 적습니다 — `전용 60㎡ (18.1평 · 공급 약 23평)`. 공급은
자료에 없어 전용률로 되짚은 **추정치**라 늘 "약" 을 붙이고, **필터와 정렬은
전용면적으로만** 합니다. 추정치로 거르면 없는 정확도를 만드는 셈입니다.

### 자료로 못 답하는 것을 자료인 척하지 않습니다

- **방 개수가 없습니다.** 전용면적으로 대신 잽니다 — 원룸·투룸 경계가 36㎡,
  방 둘이 안정적으로 나오는 선이 45㎡(공급 약 17평)입니다. 그렇게 적습니다.
- **반려동물 가능 여부는 어느 공공자료에도 없습니다.** 단지별 메모 칸을 두고
  관리사무소에 확인할 것을 목록으로 냅니다 — 엘리베이터·산책 동선·바닥재·주차.
- **출퇴근 소요시간을 지어내지 않습니다.** 지도 API 가 없어 분 단위를 낼 수
  없으므로 **방향 등급**(지사 소재지/가까움/보통/멂)과 근거 문장만 둡니다.
  내서읍이 함안 칠원과 맞붙어 있다는 것은 사실이고, "22분" 은 지어낸 값입니다.

### 얇은 표본이 1등으로 올라오면 안 됩니다

거래 1건짜리 중위가는 시세가 아니라 **그 집 한 채 가격**입니다. 실제로 전세
1건(3,000만)짜리가 목록 맨 위에 올라와 "이 단지가 제일 싸다" 로 읽혔습니다.
최소 거래 건수를 **전세·월세 각각에** 겁니다 (기본 2건) — 평형 전체 건수로만
걸면 "전세 1건 + 월세 6건" 평형에서 전세 1건이 그대로 대표 시세가 됩니다.

부호를 문장으로 옮길 때 전제를 깔지 않습니다. 전세 보증금이 월세 보증금보다
**작은** 단지가 실제로 있어(LH·구축 소형), "전세가 더 묶인다" 고 써 두면
"−2,992만원 더 묶임" 이 나옵니다.

### 전세대출은 한도가 아니라 자격에서 떨어집니다

매수 쪽 주담대는 LTV·DSR 이 축이라 "얼마까지" 가 문제인데, 전세대출은
**되느냐 안 되느냐**가 먼저입니다. 그래서 `loans.ts` 는 탈락 사유를 **전부**
모읍니다 — 하나만 보여 주면 "소득만 낮추면 되나" 로 읽히는데 실제로는 나이·
세대주·보증금 상한이 같이 걸려 있는 경우가 많습니다.

```
한도 = min(보증금 × 상품별 비율, 상품 절대상한)
```

**동거는 신혼이 아닙니다.** 혼인신고 전이면 신혼부부 전용 상품의 자격이 없고,
대신 **소득을 합산하지 않습니다.** 둘 중 소득이 낮은 사람이 세대주로 신청하면
자격 문턱을 넘기 쉬워지는 갈림길이라, 계산이 아니라 문장으로 결과에 남깁니다.

상품 순서는 **되는 것을 금리순으로 먼저, 안 되는 것을 뒤에** 둡니다. 안 되는
것을 숨기지 않는 이유는 그 목록이 곧 "무엇을 바꾸면 되는가" 이기 때문입니다 —
혼인신고·중소기업 재직·보증금 낮추기가 전부 거기서 나옵니다.

### 찾기와 후보판의 역할 분담 — 겹치면 사용자만 한 번 더 돈다

후보판이 생기면서 찾기 화면의 거르기·정렬·후보 카드·빠른 조건은 **겹치는 기능**이
됐습니다. 그대로 두면 사용자는 같은 일을 두 화면에서 합니다. 그래서 찾기 화면을
후보판에 없는 것 두 가지만 하도록 다시 짰습니다 (`RentFinderApp.tsx`, 계산은 `boardRow.ts`).

```
① 내 조건        나이·소득·혼인·재직 + 보증금에 넣을 내 돈 + 월 주거비 한도
② 얼마짜리까지   상품별 최대 보증금 = min(내 돈 ÷ (1−비율), 내 돈 + 상품 상한, 보증금 상한)
③ 발굴           창원 5개 구 + 함안·의령 전체에서 후보판에 없는 집 중 내 돈·월 한도로 되는 것만
                 → "후보판에 담기" → 후보판에 "직접 담음" 으로 들어감
④ 대출 자세히    보증금 하나로 상품별 자격·한도·탈락 사유 (예전 대출 비교 표)
```

- **내 조건이 후보판의 대출 칸을 다시 잽니다.** 후보판 스냅샷은 "32세 · 소득 4천만"
  가정으로 계산돼 있어 "내가 이 집에 들어가려면 내 돈이 얼마" 에 답하지 못했습니다.
  `BoardFrame` 이 탭을 열 때 찾기의 조건(`masan-rent-finder-v1`)과 담은 집
  (`masan-board-added`)을 읽어 `personalizeRows()` 로 다시 조립합니다.
- **담은 집은 id(`단지id_면적`)만 저장합니다.** 줄은 열 때마다 `boardRowById()` 로
  스냅샷 규칙 그대로 다시 만들고(전세가율·판정·최근 전세), 스냅샷 줄과 섞어 자동 순위를
  다시 셉니다. 도로거리·인프라 자동 점수·지도는 스냅샷에만 있어 새 줄은 비어 있고,
  인프라는 후보판에서 직접 매깁니다.
- **"돌려받기 쉬운 순" 은 판정 → 전세가율** 입니다. 전세가율만으로 세우면 표본이 얇은
  "판단 보류" 집이 낮은 비율로 맨 위에 올라옵니다.
- 새 줄의 최근 전세는 `recentJeonse()` 가 스냅샷과 같은 규칙으로 냅니다(최근 3분기 →
  4분기 → 6분기 → 2년, 중위의 70% 미만은 특수 계약으로 제외, 3건 이하가 1.5배 넘게 갈리면
  시세 불명). 스냅샷과 같은 판정이 나오는지 테스트가 대조합니다.

**이 룰셋의 수치는 추정입니다.** 주택도시기금 상품의 소득·순자산 요건과 금리
구간은 수시로 바뀝니다. `disclaimer` 와 상품별 `basis` 를 화면에 상시 노출하고,
공고로 재확인하라고 적습니다 — 매수 룰셋과 같은 규율입니다.

### 전세는 싼가보다 돌려받느냐가 먼저입니다

`safety.ts` 가 전세 후보마다 **같은 단지·평형 매매 중위가**(`market-*.json`,
같은 `aptSeq`)와 맞대 셋을 냅니다. 경계·낙찰가율 같은 수치는 전부
`rent-loans-*.json` 의 `jeonseSafety` 에 있습니다.

```
전세가율       = 보증금 ÷ 매매 중위가(최근 4분기 묶음)  (60·70·80% 에서 끊음)
근저당 허용선   = 매매 중위가 × 낙찰가율 − 보증금  (낙찰가율 80% 가정)
매매가 추세    = 최근 분기 ÷ 4분기 이상 앞선 분기 − 1
```

**융자(근저당)가 있는지는 어느 공개 API 에도 없습니다.** 등기부는 호수 단위
인터넷등기소 열람뿐이고, 공시가격 API(NSDI)는 폐기됐고, 건축물대장 API 는
이 키로 활용신청이 안 돼 있습니다. 그래서 "융자가 있나" 대신 **"융자가 얼마까지면
괜찮은가"** 를 냅니다 — 중개사가 등기부를 보여 줄 때 을구 채권최고액 합계를
허용선과 견주면 됩니다. 허용선이 음수면 "근저당 없어도 부족" 으로 말하고,
화면 하단에 등기부·납세증명·잔금일 재확인 순서표(`REGISTRY_CHECKLIST`)를 둡니다.

**갭투자 흔적은 재 봤지만 뺐습니다.** 매매 뒤 120일 안에 같은 단지·평형·층에서
신규 전세가 체결된 건을 셌더니 2년 4,502건, 그런데 동·호수가 없어 같은 층 다른
호가 우연히 겹칠 기대치가 4,257건이었습니다. 6% 차이는 신호가 아니라 잡음이라
화면에 올리지 않았고, 재현 스크립트(`scripts/fetch-masan-landlord.mjs`)만 남깁니다.

**두 건의 평균이 시세 행세를 하면 안 됩니다.** 창원메트로시티석전 52㎡ 는 전세가
2.50억·0.34억 두 건이라 중위(=평균) 1.42억이 되어 전세가율 42% 로 '안전한 전세'
1위에 올라왔습니다. 전세 3건 이하인데 분기 중위가 1.5배 넘게 갈리면(`jeonseSplit`)
전세가율을 내지 않고 "갈림" 이라고 적습니다.

이 판정·전세가율·근저당 허용선이 지금은 후보판과 찾기 화면 ③에 같이 쓰입니다.
최우선변제(보증금 7,500만 · 2,500만)와 임대 전용 단지 기준(월세 30건↑ · 매매 0건)은
`rent-loans-*.json` 의 `jeonseSafety` 에 있습니다.

### 전·월세 후보판 — `＋ 청약 · 전월세 → 전·월세 후보판`

좁힌 후보를 놓고 **소거 · 순위 고치기 · 판정 고치기 · 리모델링 표시 · 인프라 점수 · 메모 ·
위치 지도 · 다중 필터 · 정렬 우선순위 · 저장한 보기**를 하는 판입니다. 전세(34)와 월세(91)를
후보판 안의 왼쪽 사이드바로 오갑니다.

**React 로 다시 짜지 않고 원본 HTML 하나를 iframe srcdoc 으로 싣습니다.** 같은 원본을
claude.ai 아티팩트로도 쓰기 때문입니다(아티팩트는 공유 DB, 사이트는 이 브라우저 저장).
원본은 `board/template.html` 이고, 고친 뒤 `node scripts/board/sync-template.mjs` 로
`template.ts` 를 다시 만듭니다 — 어긋나면 테스트가 잡습니다. 두 번째 엔트리를 만들지
않는 것이 이 방식의 이유입니다 (위 "엔트리는 하나" 참고).

- **외부 요청 0건을 지킵니다.** 아티팩트판의 구글 글꼴은 사이트판에서 빼고, 지도는 OSM
  타일을 data URI 로 담았습니다(동네 z15 · 전체 z11, 약 1.4MB). 그래서 단일 HTML 이
  4.2MB → 5.8MB 로 커졌습니다.
- **세로 스크롤은 페이지 하나로만.** 표 상자가 따로 세로 스크롤하면 끝에서 막혀 위로 안
  올라가고, 머리줄이 상자와 함께 화면 밖으로 밀립니다(iframe 안에서 실제로 그랬음). 그래서
  표 상자는 가로로만 스크롤하고, 머리줄은 화면 맨 위에 복제해 띄우며(`#floatHead`),
  가로 스크롤은 화면 아래 막대(`#hbar`)로 대신합니다. 왼쪽 고정 두 칸(소거·단지)의 복제는
  실제 sticky 위치와 같게 옮깁니다.
- **사이트는 공유 저장이 없습니다.** 소거·메모는 그 브라우저에만 남고, 기기를 옮길 때는
  사이드바 "설정 옮기기" 로 JSON 파일을 내보내고 가져옵니다. 처음 여는 브라우저는
  스냅샷의 `seed`(아티팩트에서 해 둔 소거 17개·순위·가중치·보기)로 시작합니다.
- **거리·시간은 신호·정체를 뺀 최소치**(FOSSGIS OSRM)이고, 기준점은 함안의령지사
  (함안군 가야읍)입니다. 좌표는 도로 단위라 수백 m 어긋납니다.
- **인프라 자동 점수는 후보끼리의 상대 점수**(OSM 시설 수 5분위)입니다. 한국 OSM 은
  의원이 많이 빠져, 0곳이 40% 넘는 축은 0을 점수로 만들지 않고 비워 둡니다.
- **리모델링 여부는 공개 자료에 없습니다.** 구축(기본 2011년 이전)만 사람이 표시하고,
  "구축은 리모델링 된 것만" 필터가 확인 전인 구축도 거릅니다.
- **오피스텔은 아직 없습니다.** 국토부 오피스텔 전월세·매매 API 는 이 키로 활용신청이
  안 돼 있습니다(`SERVICE_KEY_IS_NOT_REGISTERED` — 같은 키로 아파트는 됨을 대조함).

### 함안·의령에는 살 집이 거의 없습니다

아파트 전월세 거래가 함안 213건/2년, 의령 38건/2년입니다. 그래서 **마산에 살며
그쪽으로 출퇴근** 하는 그림이 맞고, 두 군은 참고용으로만 담습니다. 0건이
오류가 아니라 사실이므로 지역별 건수를 화면에 그대로 냅니다.

## 컨벤션

- UI 문구·주석·커밋 메시지 모두 **한국어**.
- 금액은 `engine/format.ts` 의 `money()` 로 표기 (억/만원 자동 전환).
- 숫자가 열로 정렬되는 곳엔 `tabular-nums`.
- 새 도메인 규칙을 넣으면 테스트를 같이 씁니다.
