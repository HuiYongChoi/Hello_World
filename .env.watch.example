# 전세 신호 메일 설정 — 이 파일을 .env.watch 로 복사해 채웁니다 (.env.watch 는 저장소에 안 올라감)
#
# Gmail 이면: Google 계정 → 보안 → 2단계 인증 켜기 → "앱 비밀번호" 에서 16자리 비밀번호 발급
# 그 16자리를 SMTP_PASS 에 넣습니다 (계정 비밀번호가 아닙니다).
SMTP_USER=보내는주소@gmail.com
SMTP_PASS=앱비밀번호16자리
MAIL_TO=받을주소@gmail.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
